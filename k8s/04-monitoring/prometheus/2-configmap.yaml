apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: jenkins
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: 'pm25_backend'
        static_configs:
          - targets: ['backend:8000']