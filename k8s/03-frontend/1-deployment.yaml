apiVersion: apps/v1
kind: Deployment
metadata:
  name: pm25-frontend
  namespace: jenkins
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: rubchek/pm25-frontend:latest